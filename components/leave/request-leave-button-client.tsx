"use client"

import React from "react"
import { RequestLeaveButton } from "./request-leave-button"

export default function RequestLeaveButtonClient(props: any) {
  return <RequestLeaveButton {...props} />
}
